# v0.4.29 · 下载进度条

「下载新版 APK」按钮下方新增下载百分比、横向进度条及已下载 / 总大小。点击下载后，按钮立即置灰且不可重复点击；取消或失败后恢复可用。

进度按实际收到的字节更新，每 500 毫秒最多刷新一次。切换镜像时从 0% 重新显示，下载结束补齐 100%，随后提示正在校验 APK。保留镜像回退、完整性与签名校验、系统安装确认。

版本 **0.4.29（versionCode 86）**，与当前 0.4.27 / 0.4.28 测试及发布版同签名，可覆盖升级并保留设置和配对。安装后重新开启无障碍。

**旧版下载本次更新时仍使用旧界面；升级到 0.4.29 后，新进度条才会在后续下载中显示。** 本次仅发布，不直接安装到测试手机。

验证：模型回归测试、Android 构建与 Lint 通过（0 errors / 29 warnings）；新增实际字节范围、镜像切换归零和快速下载完成 100% 的回调检查。未在用户手机上安装本版进行界面验证。
